任务描述
优化下面这段python代码，并尽可能提高cal函数的运行速度。

名词解释：

n：自由度，正整数
L(q, qdot, usermath)：一个简单的纯函数，对q[i]和qdot[i]执行一系列固定的计算，这些计算包含加，减，乘，取负，以及sin，cos这两种三角函数
cal 函数参数：

q：广义变量，长度为n的64位浮点numpy数组
qdot：广义变量随时间的导数，长度为n的64位浮点numpy数组
qddot：广义变量随时间的二次导数，长度为n的64位浮点numpy数组
L(q, qdot, usermath)参数：

q：可以__getitem__，q[i]为第i个广义变量
qdot：可以__getitem__，qdot[i]为第i个广义变量随时间的导数
usermath：用户提供的数学库，需要包含sin和cos两种函数

测试
需要提交一份Python代码，内容为优化后的gc函数
这个任务确实无法有效利用多线程并行计算，因此本题将在 Intel Xeon Platinum 8358 CPU 的单个核心上运行。
仅有一个测试样例，自由度 n = 20，cal调用次数 150000
计算总时限 300 s
目标时间 1.0s
分数正比于运行时间的倒数，score = 100/time
注意
时间演化的固有的运行时间也会计算在内，这部分大致在 0.35 s 左右。
Python 本身仅额外安装了numpy，但可以使用系统自带的工具和库，具体内容参考base镜像，额外包含python3-devel。
不需要考虑稀疏性的事。
不要试图拿到L函数并在提交中硬编码相关内容。
gc函数的执行时间和后续计算共享时限，没有额外的限制，不参与评分。
不建议在gc函数中访问外部内容，比如 pip install

把答案写在ans.py里面